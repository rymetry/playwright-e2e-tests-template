<!--
このファイルは新規Test Design Doc作成用の汎用テンプレートです。
命名規則・Status・昇格条件の詳細は test-designs/README.md を参照してください。

使い方:
1. test-designs/<level>/<area>/<Parent Case ID>-<slug>.md へコピーする
2. ID、機能名、Check名、Tierを置き換える
3. 使用しないExecution modeのCheckブロック（3.x）と、Check一覧の対応行を削除する
4. 各Checkの必須見出しは残し、該当しない項目は「なし」または「対象外（理由）」とする
5. Checkの初期StatusはDRAFTとする。レビュー済みの期待値をテストまたは手順として
   実装したらEVALUATINGへ、README 4.1の昇格条件を満たしたらACTIVEへ変更する
6. live UIの観測結果、Locator候補、生成コードを無審査で期待値にしない
7. 固定wait／sleep、skip、自己修復処理、不安定なCSS／XPath、秘密情報を
   正式なTest Designやテストコードへ移さない。座標依存操作は原則使用せず、
   COMPUTER_USEで不可避な場合だけ3.3の記録条件付きで許容する
8. 置換と不要Checkの削除が完了したら、このコメントを削除する
-->

# XXX-AREA-000 機能名

## メタデータ

| 項目 | 値 |
|---|---|
| Parent Case ID | XXX-AREA-000 |
| テストレベル | E2E／INT |
| 機能 | 機能名 |
| 対象環境 | `E2E_BASE_URL`で指定する環境の説明 |
| 最終確認 | YYYY-MM-DD / 実行証跡 |

## Check一覧

| Check ID | Execution mode | Exploration mode | Tier | Status | Code / 手順 |
|---|---|---|---|---|---|
| XXX-AREA-000-PW-01 | PLAYWRIGHT | `PLAYWRIGHT_CLI`／`NONE` | SMOKE | DRAFT | `e2e/<area>/XXX-AREA-000.spec.ts` |
| XXX-AREA-000-API-01 | API | `API_INTEGRATION`／`NONE` | REGRESSION | DRAFT | `e2e/<area>/XXX-AREA-000.spec.ts` |
| XXX-AREA-000-CU-01 | COMPUTER_USE | `COMPUTER_USE`／`NONE` | REGRESSION | DRAFT | 本書3.3の手順 |
| XXX-AREA-000-MN-01 | MANUAL | `MANUAL`／`NONE` | REGRESSION | DRAFT | 本書3.4の手順 |

ID、mode、Tier、Statusの選択規則は`test-designs/README.md`の2〜4章と6章を参照する。

## 1. 目的

このParent Caseが保証するユーザー価値・システム価値を記載する。

## 2. 品質リスク

このシナリオが壊れたときに何が起きるかを、ケース固有の内容で記載する。

- 対象操作が完了しない
- 誤った権限または利用者として操作できる
- 永続状態がレビュー済みの期待値と一致しない
- 利用者が重要な結果を確認できない

## 3. Check設計

探索サマリの項目定義、状態別の値、レビュー手順は`test-designs/README.md`の6章を
参照する。

### 3.1 XXX-AREA-000-PW-01: Check名

#### 前提条件

テスト開始前から成立していなければならない条件を記載する。不一致の場合は
推測で修復せず、対象操作を開始する前に停止する。

- `E2E_BASE_URL`のoriginが`E2E_ALLOWED_ORIGINS`に含まれる
  （`playwright.config.ts`が起動時に検証する）
- 認証状態またはケース固有の開始状態が成立している
- 利用するアカウント、権限、データの識別条件が成立している

#### テストデータ

| 項目 | 値・生成方法 |
|---|---|
| 入力値 | なし、またはケース固有の値 |
| 動的データ | なし、または生成・識別方法 |
| 競合回避 | 対象外、または一意性を保証する方法 |

#### Fixture

| Fixture | 用途 |
|---|---|
| なし | このCheckは事前準備済みデータに依存しない |

Check開始前から存在するアカウント、認証状態、データ、ファイル、外部状態だけを
記載する。再利用が必要になった時点で共通Fixtureへ分離する。

#### 前処理

テスト自身がシナリオ実行前に行う準備を記載する。期待結果とは分離する。

- なし
- または、開始画面への移動やテストデータの準備

#### シナリオ

Given:

- 利用者が宣言済みの開始状態にいる

When:

- 利用者が対象操作を行う

Then:

- レビュー済みの期待結果を確認できる
- 必要な永続状態が再読み込み後も維持される

#### Assertion設計

##### Functional

- 操作結果
- URL、表示、保存状態
- 再読み込み後の状態

##### Accessibility

- 主要要素のroleとaccessible name
- 状態を表すARIA属性
- 対象外の場合は理由

##### Visual

- VRT対象、または対象外の理由
- mask、対象領域、安定化条件

#### 後処理

| 結果 | 後処理 |
|---|---|
| PASS | なし、または作成データを安全に削除する |
| FAIL | 必要最小限の証跡を保持し、原因確認前に再実行しない |

#### 実行契約

| 項目 | 値 |
|---|---|
| Playwright Project | `chromium`、またはプロジェクト固有の値 |
| Tierタグ | SMOKEは`{ tag: '@smoke' }`オプションで付与。非SMOKEは現状タグなし（README 7章のトリガー発生まで`@regression`等は導入しない） |
| QUARANTINE時 | `{ tag: '@quarantine' }`を付与し、通常実行から除外する |
| 最大待機時間 | Playwright既定値、またはケース固有の上限 |
| ポーリング | 対象外、または完了条件と間隔 |
| retry | 通常実行はrepository設定に従い、qualificationでは`0` |
| 並列実行 | repository設定に従う。ケース固有の制約があれば記載 |
| 失敗時証跡 | trace、screenshot、URL origin、実行日時、最後に成功した操作 |

ケース固有の制約:

- 固定時間待機を使わず、観測可能な完了条件で判定する
- retryやskipで期待値との不一致を隠さない
- 更新操作より前に対象環境とoriginの一致を確認する
- Locatorはrole、label、test IDなど安定したセマンティクスを優先する

#### 探索目的

- 到達経路と状態遷移
- 安定Locator候補
- loading、polling、animationなどの待機条件
- 外部依存、失敗しやすい操作、Assertion候補
- 探索不要の場合は`対象外（理由）`

#### 探索サマリ

| 項目 | 値 |
|---|---|
| Exploration mode | `NONE`／`PLAYWRIGHT_CLI` |
| Run / 観測環境 | 未実施 |
| 観測サマリ | 未記入（探索後に本記入） |
| 実装候補（レビュー対象） | 未記入（探索後に本記入） |
| 観測上の疑問・要判断 | 未記入（探索後に本記入） |
| Artifacts | なし |

#### レビュー済みの期待値

| 項目 | 値 |
|---|---|
| 根拠 | 仕様、Issue、受入条件、またはレビュー記録 |
| レビュー日 | YYYY-MM-DD |
| レビュー担当 | 担当者または役割 |
| 期待値 | 正式なAssertionとして実装する内容の要約 |

#### Test Status判定根拠

| 項目 | 値 |
|---|---|
| 判定 | DRAFT／EVALUATING／ACTIVE／QUARANTINE／RETIRED |
| 判定日 | YYYY-MM-DD |
| 判定根拠 | 現在のStatusにした理由 |
| Qualification command / procedure | 未実施、または実際に使用したコマンド・手順 |
| Qualification result | 未実施、または検証結果の要約 |
| 実行条件 | 対象環境／origin、Playwrightとbrowserのversion、主要な設定 |
| 対象revision | commit SHA等。Git未管理の場合はその旨を記載 |
| 証跡 | 本表の記録を一次証跡とする。補助として`qualification-reports/`のパス、trace、Issue／PR等への参照 |

昇格条件はREADME 4.1に従う。1回でも失敗、skip、設定変更がある場合は
EVALUATINGを維持し、原因を記録する。

#### 対象外・未確定

- このCheckでは保証しない内容
- レビューまたは仕様責任者の判断を待つ内容

### 3.2 XXX-AREA-000-API-01: Check名

3.1と同じ構造で記載する。ブラウザ非依存のため、次の点を読み替える。

- Assertion設計は Functional に加えて次を対象とする
  - HTTP status、response schema、エラー時のレスポンス形式
  - 副作用（永続状態、イベント、通知）の確認方法
  - AccessibilityとVisualは`対象外（API Checkのため）`とする
- 実行契約のPlaywright Projectは`api`等のブラウザ非依存Project、
  またはAPIクライアント設定を記載する

追加で次を明記する。

- 対象endpoint、認証方式（秘密情報は書かない）、依存する外部サービス
- UIテスト（PW Check）との役割分担。同じ保証を二重に持たない

### 3.3 XXX-AREA-000-CU-01: Check名

#### 自動化できない理由

Playwrightではなく画面操作で実行する理由を記載する（Canvas描画、OS UI、
ブラウザ拡張機能、複数アプリ間の連携など）。

#### 前提条件

- 使用するアプリ、ブラウザ、OS、拡張機能とそのversion
- 画面条件（解像度、表示倍率、ウィンドウ状態）
- 認証状態またはケース固有の開始状態
- 操作開始前に、表示中のURL originが許可済み環境と一致することを確認する

#### テストデータ

| 項目 | 値・生成方法 |
|---|---|
| 入力値 | なし、またはケース固有の値 |
| 動的データ | なし、または生成・識別方法 |

#### Fixture

| Fixture | 用途 |
|---|---|
| なし | このCheckは事前準備済みデータに依存しない |

#### 前処理

- なし
- または、開始画面の表示やテストデータの準備

#### 操作手順

実行エージェントがそのまま実行できる粒度で記載する。

1. 開始画面と開始状態
2. 操作ステップ（1ステップ1操作。各ステップに観測可能な完了条件を書く）
3. 判定ステップ（何を見て合否を決めるか）

手順は座標ではなく、利用者が識別できる画面要素と操作結果で記述する。
座標依存操作が不可避な場合だけ、次を記録して許容する。

- 使用理由
- 解像度、表示倍率、ウィンドウ位置
- 基準となる画像またはアンカー要素
- 操作後の観測可能な完了条件

#### 判定基準

- Computer Useが判定できる箇所（画面上の要素、表示文言、状態変化）
- 人の判断が必要な箇所と、その判定基準
- 主観的な表現を避け、観測可能な条件で記載する

#### 後処理

| 結果 | 後処理 |
|---|---|
| PASS | なし、または作成データを安全に削除する |
| FAIL | screenshot等の証跡を保持し、原因確認前に再実行しない |

#### 実行契約

| 項目 | 値 |
|---|---|
| 使用ツール | Computer Use環境とversion |
| 実行者／エージェント | 実行主体の識別 |
| 最大所要時間 | 目安時間。超過した場合は中断して記録する |
| 証跡 | screenshot、動画、操作ログなど必要最小限のもの |

#### 探索目的

- 到達経路、画面状態、操作の完了条件
- 人の判断が必要な箇所の特定
- 探索不要の場合は`対象外（理由）`

#### 探索サマリ

| 項目 | 値 |
|---|---|
| Exploration mode | `NONE`／`COMPUTER_USE` |
| Run / 観測環境 | 未実施 |
| 観測サマリ | 未記入（探索後に本記入） |
| 実装候補（レビュー対象） | 未記入（探索後に本記入） |
| 観測上の疑問・要判断 | 未記入（探索後に本記入） |
| Artifacts | なし |

#### レビュー済みの期待値

| 項目 | 値 |
|---|---|
| 根拠 | 仕様、Issue、受入条件、またはレビュー記録 |
| レビュー日 | YYYY-MM-DD |
| レビュー担当 | 担当者または役割 |
| 期待値 | 判定基準として使用する内容の要約 |

#### Test Status判定根拠

| 項目 | 値 |
|---|---|
| 判定 | DRAFT／EVALUATING／ACTIVE／QUARANTINE／RETIRED |
| 判定日 | YYYY-MM-DD |
| 判定根拠 | 現在のStatusにした理由 |
| Qualification procedure | 未実施、または実際に使用した手順 |
| Qualification result | 未実施、または検証結果の要約 |
| 実行条件 | 対象環境、使用ツール、実行者／エージェント |
| 証跡 | 本表の記録を一次証跡とし、補助としてscreenshot、操作ログ等への参照 |

#### 対象外・未確定

- このCheckでは保証しない内容
- レビューまたは仕様責任者の判断を待つ内容

### 3.4 XXX-AREA-000-MN-01: Check名

#### 手動で実行する理由

自動化またはComputer Useでは扱わない理由を記載する（人の意味判断、
感性的評価、物理操作、法的・契約上の要件など）。

#### 前提条件

- 実行者に必要な知識、権限、機器、環境
- 認証状態またはケース固有の開始状態
- 操作開始前に、表示中のURL originまたは対象環境が許可済み環境と
  一致することを確認する

#### テストデータ

| 項目 | 値・生成方法 |
|---|---|
| 入力値 | なし、またはケース固有の値 |
| 動的データ | なし、または生成・識別方法 |

#### Fixture

| Fixture | 用途 |
|---|---|
| なし | このCheckは事前準備済みデータに依存しない |

#### 前処理

- なし
- または、開始画面の表示やテストデータの準備

#### 操作手順

実行者がそのまま実行できる粒度で記載する。

1. 開始画面と開始状態
2. 操作ステップ
3. 判定ステップ

#### 判定基準

- 主観的な表現を避けた基準（「見やすい」ではなく「○○が△△の状態である」）
- 判定に迷った場合の扱い（FAILとするか、保留として記録するか）

#### 後処理

| 結果 | 後処理 |
|---|---|
| PASS | なし、または作成データを安全に削除する |
| FAIL | 証跡を保持し、原因確認前に再実行しない |

#### 実行契約

| 項目 | 値 |
|---|---|
| 実行者 | 必要な役割または担当者 |
| 最大所要時間 | 目安時間。超過した場合は中断して記録する |
| 記録方法 | チェックリスト、記入済み結果表、screenshotなど、合否を再確認できるもの |

#### 探索目的

- 到達経路と確認対象の特定
- 判定基準の候補
- 探索不要の場合は`対象外（理由）`

#### 探索サマリ

| 項目 | 値 |
|---|---|
| Exploration mode | `NONE`／`MANUAL` |
| Run / 観測環境 | 未実施 |
| 観測サマリ | 未記入（探索後に本記入） |
| 実装候補（レビュー対象） | 未記入（探索後に本記入） |
| 観測上の疑問・要判断 | 未記入（探索後に本記入） |
| Artifacts | なし |

#### レビュー済みの期待値

| 項目 | 値 |
|---|---|
| 根拠 | 仕様、Issue、受入条件、またはレビュー記録 |
| レビュー日 | YYYY-MM-DD |
| レビュー担当 | 担当者または役割 |
| 期待値 | 判定基準として使用する内容の要約 |

#### Test Status判定根拠

| 項目 | 値 |
|---|---|
| 判定 | DRAFT／EVALUATING／ACTIVE／QUARANTINE／RETIRED |
| 判定日 | YYYY-MM-DD |
| 判定根拠 | 現在のStatusにした理由 |
| Qualification procedure | 未実施、または実際に使用した手順 |
| Qualification result | 未実施、または検証結果の要約 |
| 実行条件 | 対象環境、実行者 |
| 証跡 | 本表の記録を一次証跡とし、補助として記入済み結果表、screenshot等への参照 |

#### 対象外・未確定

- このCheckでは保証しない内容
- レビューまたは仕様責任者の判断を待つ内容

## 4. 関連仕様

- 対象機能の仕様
- 関連Issue／PR
- 期待値レビュー記録
- 関連するTest Design
